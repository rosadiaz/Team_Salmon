<h1> <%= @user.nickname.titleize %>'s Account Details </h1>

<p class="mx-2"> Name: <%= @user.first_name %> <%= @user.last_name %></p>

<p class="mx-2"> Taken quizzes: <%= @total_taken_quizzes %></p>

<p class="mx-2"> Your created quizzes: <%= @total_created_quizzes %></p>

<p class="mx-2"> Your total points: <%= @total_score %></p>

<div class="mx-2">
  <%= link_to('Edit', edit_user_path(@user), class: "btn btn-outline-primary")%>
  <%= link_to('Delete', user_path(@user), method: :delete, class: "btn btn-outline-primary") %>
</div>
