<h1><%= @quiz.title %></h1>

<p><%= @quiz.description %></p>
<div>
  <%= link_to 'Edit', edit_quiz_path(@quiz)%>
  <%= link_to 'Delete', quiz_path(@quiz), method: :delete %>
</div>

<div>
  <%= link_to 'Add Questions', new_question_path %>
</div>